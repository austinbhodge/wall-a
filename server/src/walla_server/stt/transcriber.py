"""Whisper speech-to-text pipeline."""

import logging

log = logging.getLogger(__name__)

# TODO: Load Whisper model, transcribe audio chunks from Pi mic
# import whisper
# model = whisper.load_model("base")
