"""Audio I/O — microphone capture and speaker playback."""

import logging

log = logging.getLogger(__name__)

# TODO: Implement audio capture (USB mic) and playback (speaker)
# Options: pyaudio, sounddevice, or subprocess to arecord/aplay
